export default function Navbar() {
  return (
    <div className="navbar">
      {/* <button
        className="button"
        style={{ width: "120px" }}
        onClick={() => {
          localStorage.clear();
          window.location.href = "/";
        }}
      >
        Logout
      </button> */}
    </div>
  );
}
